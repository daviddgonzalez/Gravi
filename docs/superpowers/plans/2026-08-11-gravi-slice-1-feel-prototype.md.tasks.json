{
  "planPath": "docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Repo skeleton and async frame loop",
      "status": "completed",
      "description": "**Goal:** A `pip install -e .[dev]` package with a pytest suite and an async pygame window that opens, clears to the neon background, and closes cleanly.\n\n**Files:**\n- Create: `pyproject.toml`\n- Create: `main.py`\n- Create: `src/gravi/__init__.py`\n- Create: `src/gravi/config.py`\n- Create: `tests/test_config.py`\n\n**Acceptance Criteria:**\n- [ ] `pip install -e .[dev]` succeeds\n- [ ] `pytest -q` runs and passes (2 tests)\n- [ ] `python main.py` opens a 1280x720 window filled with COLOR_BG and closes on window-close or Escape\n- [ ] `main()` is an `async def` driven by `asyncio.run`, with `await asyncio.sleep(0)` once per frame\n- [ ] `src/gravi/__init__.py` imports nothing\n\n**Verify:** `pytest -q` -> `2 passed`, and `python main.py` opens a window that closes on Escape\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"pyproject.toml\", \"main.py\", \"src/gravi/__init__.py\", \"src/gravi/config.py\", \"tests/test_config.py\"], \"verifyCommand\": \"pytest -q\", \"acceptanceCriteria\": [\"pip install -e .[dev] succeeds\", \"pytest -q reports 2 passed\", \"python main.py opens a 1280x720 window that closes on Escape\", \"main() is async with await asyncio.sleep(0) once per frame\", \"src/gravi/__init__.py imports nothing\"], \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Prove the browser deployment path",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Confirm the blank window from Task 1 builds with pygbag and runs in a real browser, before any game code is written on top of the stack. If pygbag cannot package the app, STOP and raise it \u2014 the stack decision and every later task depend on the answer.\n\n**USER-ORDERED GATE \u2014 NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `docs/web-build.md`\n- Modify: `.gitignore` (add `build/`)\n\n**Acceptance Criteria:**\n- [ ] `pygbag --build .` completes without error and produces `build/web/index.html` plus a `.apk` payload\n- [ ] `pygbag .` serves the app and `http://localhost:8000` renders the near-black 1280x720 canvas in a browser\n- [ ] The browser console shows no uncaught Python traceback\n- [ ] `docs/web-build.md` records the exact build command, the serve command, and the pygbag version that worked\n\n**Verify:** `pygbag --build . && ls build/web/index.html` -> path printed; then `pygbag .` and load `http://localhost:8000` in a browser and confirm a near-black canvas with a clean console\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"docs/web-build.md\", \".gitignore\"], \"verifyCommand\": \"pygbag --build . && ls build/web/index.html\", \"acceptanceCriteria\": [\"pygbag --build . exits 0 and build/web/index.html exists\", \"http://localhost:8000 renders the near-black 1280x720 canvas in a browser\", \"browser console shows no uncaught Python traceback\", \"docs/web-build.md records build command, serve command, and the working pygbag version\"], \"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"gateScope\": \"blocks-all-subsequent-tasks\", \"failurePolicy\": \"stop-and-raise\", \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: The force law",
      "status": "pending",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** `field.py` \u2014 a pure module holding attract, repel, clamping, and the node definition, with tests that pin every branch of the maths.\n\n**Files:**\n- Create: `src/gravi/field.py`\n- Create: `tests/test_field.py`\n\n**Acceptance Criteria:**\n- [ ] Attract returns a force pointing at the node centre with magnitude `k_attract * r`\n- [ ] Repel returns a force pointing away with magnitude `k_repel * (R - r)`\n- [ ] Both return `(0.0, 0.0)` outside the influence radius and when the charge is neutral\n- [ ] Both are clamped to `force_max`\n- [ ] Attract at exactly the node centre returns `(0.0, 0.0)` rather than dividing by zero\n- [ ] `field.py` contains no pygame import\n\n**Verify:** `pytest tests/test_field.py -v` -> 8 passed\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"src/gravi/field.py\", \"tests/test_field.py\"], \"verifyCommand\": \"pytest tests/test_field.py -v\", \"acceptanceCriteria\": [\"attract force points at the node with magnitude k_attract*r\", \"repel force points away with magnitude k_repel*(R-r)\", \"zero force outside the influence radius and when neutral\", \"force clamped to force_max\", \"attract at the exact centre returns (0,0) without dividing by zero\", \"field.py contains no pygame import\"], \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Deterministic simulation core",
      "status": "pending",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** `sim.py` \u2014 a `World` that integrates the player under gravity plus the active node's force at a fixed timestep, selects the active node, and detects death, with a test proving two identical runs produce byte-identical traces.\n\n**Files:**\n- Create: `src/gravi/room.py`\n- Create: `src/gravi/sim.py`\n- Create: `tests/test_sim.py`\n- Create: `tests/test_purity.py`\n\n**Acceptance Criteria:**\n- [ ] `World.step()` advances one fixed timestep using semi-implicit (symplectic) Euler\n- [ ] The active node is the nearest node whose influence radius contains the player, ties broken by lowest index; `None` when outside all radii\n- [ ] Only the active node contributes force\n- [ ] Player dies on contact with a node core, and when leaving the room bounds\n- [ ] Two `World`s stepped with the same charge sequence produce identical position traces under exact `==`\n- [ ] A circular orbit stays within a 90-110px band for 10 simulated seconds\n- [ ] Importing `gravi.sim` does not import pygame\n\n**Verify:** `pytest tests/test_sim.py tests/test_purity.py -v` -> 13 passed\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"src/gravi/room.py\", \"src/gravi/sim.py\", \"tests/test_sim.py\", \"tests/test_purity.py\"], \"verifyCommand\": \"pytest tests/test_sim.py tests/test_purity.py -v\", \"acceptanceCriteria\": [\"World.step advances one fixed timestep with semi-implicit Euler\", \"active node is nearest containing node, ties on lowest index, None outside all radii\", \"only the active node contributes force\", \"death on node-core contact and on leaving room bounds\", \"two identical charge sequences produce identical traces under exact equality\", \"a circular orbit stays within a 90-110px band for 10 simulated seconds\", \"importing gravi.sim does not import pygame\"], \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: Neon rendering \u2014 nodes, beam, player, trail",
      "status": "pending",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** Draw the world as emitted light: pre-baked additive glow sprites, an influence-radius ring, a lethal core, a beam whose thickness tracks force magnitude, and a fading motion trail.\n\n**Files:**\n- Create: `src/gravi/render/__init__.py`\n- Create: `src/gravi/render/trail.py`\n- Create: `src/gravi/render/neon.py`\n- Create: `tests/test_trail.py`\n\n**Acceptance Criteria:**\n- [ ] Glow sprites are baked once per (colour, radius) and cached; no per-frame gradient generation\n- [ ] Influence-radius rings are baked and cached per (radius, alpha); no per-frame Surface allocation\n- [ ] Full-screen alpha layers use a single shared scratch buffer, not a per-call allocation\n- [ ] A node draws as a dim influence ring at `radius` plus a bright core disc at `core_radius`\n- [ ] The beam is drawn only when a node is active and the charge is not neutral, cyan for attract and pink for repel, width scaled by force magnitude\n- [ ] The trail keeps at most `TRAIL_MAX_POINTS` points and fades from tail to head\n- [ ] `trail.py` has no pygame import and is unit-tested\n\n**Verify:** `pytest tests/test_trail.py -v` -> 3 passed\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"src/gravi/render/__init__.py\", \"src/gravi/render/trail.py\", \"src/gravi/render/neon.py\", \"tests/test_trail.py\"], \"verifyCommand\": \"pytest tests/test_trail.py -v\", \"acceptanceCriteria\": [\"glow sprites baked once per (colour, radius) and cached\", \"influence rings baked and cached per (radius, alpha)\", \"full-screen alpha layers use one shared scratch buffer\", \"node draws as influence ring plus bright lethal core\", \"beam drawn only when a node is active and charge is not neutral, width scaled by force magnitude\", \"trail capped at TRAIL_MAX_POINTS and fades tail to head\", \"trail.py has no pygame import\"], \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 6: Playable loop \u2014 control, death, instant restart",
      "status": "pending",
      "blockedBy": [
        4,
        5
      ],
      "description": "**Goal:** Wire input, simulation, and rendering into a loop you can actually play: hold to attract, hold to repel, die on the core, restart instantly.\n\n**Files:**\n- Create: `rooms/slice1.json`\n- Modify: `main.py` (replace the Task 1 loop body)\n\n**Acceptance Criteria:**\n- [ ] Left mouse button or `J` attracts; right mouse button or `K` repels; holding both is neutral\n- [ ] The simulation runs at a fixed 240 Hz with an accumulator, independent of frame rate\n- [ ] Death is instant and visible; `R` restarts, and restart also clears the trail\n- [ ] Death auto-restarts after 0.6 s so the retry loop needs no input\n- [ ] The starting room has five nodes placed to allow at least one chain of three orbits\n- [ ] The frame loop still awaits `asyncio.sleep(0)`\n\n**Verify:** `pytest -q` (all prior tests still pass) and `python main.py` -> holding attract near a node swings the player around it; flying into a core restarts the run\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"main.py\", \"rooms/slice1.json\"], \"verifyCommand\": \"pytest -q\", \"acceptanceCriteria\": [\"J or LMB attracts, K or RMB repels, both held is neutral\", \"simulation runs at fixed 240 Hz via an accumulator independent of frame rate\", \"R restarts and clears the trail\", \"death auto-restarts after 0.6s\", \"starting room has five nodes allowing a chain of three orbits\", \"frame loop still awaits asyncio.sleep(0)\"], \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 7,
      "subject": "Task 7: Live tuning overlay",
      "status": "pending",
      "blockedBy": [
        6
      ],
      "description": "**Goal:** Adjust every force constant while playing, with the current values on screen and the ability to save and reload named presets.\n\n**Files:**\n- Create: `src/gravi/tuning.py`\n- Create: `src/gravi/render/hud.py`\n- Create: `presets/default.json`\n- Create: `tests/test_tuning.py`\n- Modify: `main.py`\n- Modify: `.gitignore`\n\n**Acceptance Criteria:**\n- [ ] `Tab` toggles the overlay; it starts visible\n- [ ] Up/Down select a parameter, Left/Right adjust it by its step, clamped to its bounds\n- [ ] Shift multiplies the adjustment step by 10\n- [ ] Holding Left/Right sweeps after a 0.35 s delay, without using global `pygame.key.set_repeat`\n- [ ] `F5` saves to `presets/current.json`, `F9` reloads, `F8` restores built-in defaults\n- [ ] Saving in the browser build fails silently (returns False) rather than raising\n- [ ] The overlay shows the derived orbital period `2*pi/sqrt(k_attract)`\n- [ ] `presets/current.json` is gitignored\n\n**Verify:** `pytest tests/test_tuning.py -v` -> 9 passed; then in game, select `gravity_y` and hold Left to watch the fall slow in real time\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"src/gravi/tuning.py\", \"src/gravi/render/hud.py\", \"presets/default.json\", \"tests/test_tuning.py\", \"main.py\", \".gitignore\"], \"verifyCommand\": \"pytest tests/test_tuning.py -v\", \"acceptanceCriteria\": [\"Tab toggles the overlay, visible by default\", \"Up/Down select and Left/Right adjust by the parameter step, clamped to bounds\", \"Shift multiplies the step by 10\", \"held Left/Right sweeps after 0.35s without global key repeat\", \"F5 saves, F9 loads, F8 restores defaults\", \"saving fails soft (returns False) instead of raising\", \"overlay displays the derived orbital period 2*pi/sqrt(k_attract)\", \"presets/current.json is gitignored\"], \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 8,
      "subject": "Task 8: In-session room editing",
      "status": "pending",
      "blockedBy": [
        7
      ],
      "description": "**Goal:** Drag nodes around, add and delete them, and resize their influence radius and core while playing, so the playtest is not limited to one hand-placed layout.\n\n**Files:**\n- Create: `src/gravi/editor.py`\n- Create: `tests/test_editor.py`\n- Modify: `main.py`\n- Modify: `src/gravi/render/hud.py` (help lines)\n\n**Acceptance Criteria:**\n- [ ] `Alt` + left-click within a node's core grabs it; dragging moves it; release drops it\n- [ ] Releasing Alt mid-drag does not drop the node\n- [ ] `Alt` + right-click on empty space adds a node with default radius and core\n- [ ] `Delete` removes the node under the cursor\n- [ ] `[` and `]` change the hovered node's influence radius; `,` and `.` change its core radius, each clamped\n- [ ] Core radius can never exceed half the influence radius\n- [ ] Charge input is suppressed while Alt is held, so editing never fights play\n- [ ] `Ctrl+S` saves the room to `rooms/slice1.json`, failing soft in the browser\n- [ ] Editing is pure logic in `editor.py`, tracked by index rather than by `Node` value, and unit-tested without pygame\n\n**Verify:** `pytest tests/test_editor.py -v` -> 9 passed; then in game, Alt-drag a node and watch its influence ring follow\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"src/gravi/editor.py\", \"tests/test_editor.py\", \"main.py\", \"src/gravi/render/hud.py\"], \"verifyCommand\": \"pytest tests/test_editor.py -v\", \"acceptanceCriteria\": [\"Alt+LMB grabs a node by its core and drag moves it\", \"releasing Alt mid-drag does not drop the node\", \"Alt+RMB on empty space adds a node with default radius and core\", \"Delete removes the node under the cursor\", \"[ and ] resize influence radius, , and . resize core, both clamped\", \"core radius never exceeds half the influence radius\", \"charge input suppressed while Alt is held\", \"Ctrl+S saves the room and fails soft when the filesystem is unwritable\", \"editor tracks the dragged node by index, not by Node value\"], \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    },
    {
      "id": 9,
      "subject": "Task 9: Playtest and rule on the feel",
      "status": "pending",
      "blockedBy": [
        8
      ],
      "description": "**Goal:** Play the prototype, tune it, and record a written verdict against the three feel criteria in spec section 10 \u2014 the PROCEED/STOP decision on whether Gravi continues past slice 1. This is a human judgement task; no agent can close it.\n\n**USER-ORDERED GATE \u2014 NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `docs/superpowers/runbooks/2026-08-11-slice-1-feel-verdict.md`\n- Modify: `presets/default.json` (replaced with whatever tuning actually won)\n- Modify: `src/gravi/config.py` (TUNABLES defaults updated to the winning values, if PROCEED)\n\n**Acceptance Criteria:**\n- [ ] At least three distinct tuning presets were played and compared, varying `k_attract`, `k_repel`, `gravity_y` and node radius\n- [ ] At least two hand-built node layouts beyond the shipped `rooms/slice1.json` were tested via the editor\n- [ ] Criterion 1 recorded with an explicit yes/no verdict: three orbits can be chained without deliberate thought\n- [ ] Criterion 2 recorded with an explicit yes/no verdict: approach angle visibly decides slingshot versus crash\n- [ ] Criterion 3 recorded with an explicit yes/no verdict: repel reads as a genuine save, not a panic button\n- [ ] An explicit PROCEED or STOP decision is written down, with reasoning\n- [ ] If PROCEED, the winning values are written into `presets/default.json` and the `config.TUNABLES` defaults\n- [ ] The browser build was played once, confirming the feel survives WASM frame pacing\n\n**Verify:** `docs/superpowers/runbooks/2026-08-11-slice-1-feel-verdict.md` exists and contains a verdict line for all three criteria plus a PROCEED/STOP decision; `git log` shows the preset commit\n\n**Steps:** see the plan document, `docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md`, for the full code of every step.\n\n```json:metadata\n{\"files\": [\"docs/superpowers/runbooks/2026-08-11-slice-1-feel-verdict.md\", \"presets/default.json\", \"src/gravi/config.py\"], \"verifyCommand\": \"test -f docs/superpowers/runbooks/2026-08-11-slice-1-feel-verdict.md && grep -Eq 'PROCEED|STOP' docs/superpowers/runbooks/2026-08-11-slice-1-feel-verdict.md && echo VERDICT_RECORDED\", \"acceptanceCriteria\": [\"at least three tuning presets played and compared, varying k_attract, k_repel, gravity_y and node radius\", \"at least two hand-built layouts beyond rooms/slice1.json tested via the editor\", \"criterion 1 (three orbits chained without deliberate thought) recorded with an explicit yes/no verdict\", \"criterion 2 (approach angle visibly decides slingshot versus crash) recorded with an explicit yes/no verdict\", \"criterion 3 (repel reads as a genuine save, not a panic button) recorded with an explicit yes/no verdict\", \"an explicit PROCEED or STOP decision written down with reasoning\", \"if PROCEED, winning values written into presets/default.json and config.TUNABLES defaults\", \"the browser build was played once and the feel confirmed under WASM frame pacing\"], \"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"gateScope\": \"blocks-all-later-slices\", \"failurePolicy\": \"stop-and-raise\", \"planPath\": \"docs/superpowers/plans/2026-08-11-gravi-slice-1-feel-prototype.md\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-12T17:01:24.598193+00:00"
}
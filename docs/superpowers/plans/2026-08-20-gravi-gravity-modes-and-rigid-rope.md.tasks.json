{
  "planPath": "docs/superpowers/plans/2026-08-20-gravi-gravity-modes-and-rigid-rope.md",
  "specPath": "docs/superpowers/specs/2026-08-20-gravi-gravity-modes-and-rigid-rope-design.md",
  "tasks": [
    {
      "id": 10,
      "subject": "Task 10: gravity.py \u2014 GravityMode and apply_gravity",
      "status": "completed",
      "description": "**Goal:** A pure, validator-safe function that returns the velocity after one step of gravity in a given mode.\n\n**Files:**\n- Modify: `src/gravi/gravity.py`\n- Test: `tests/test_gravity.py`\n- Modify: `tests/test_purity.py:39` (allowed-import set gains `enum`)\n\n**Acceptance Criteria:**\n- [ ] `GravityMode` has `ALONG`, `PERP_CORRIDOR`, `PERP_VELOCITY` and cycles with wraparound\n- [ ] `apply_gravity` in `ALONG` returns exactly `v + direction * magnitude * dt`\n- [ ] `PERP_VELOCITY` holds `|v|` constant to 1e-9 relative over 10,000 steps\n- [ ] `PERP_VELOCITY` turns the heading by `magnitude/|v|` radians per second\n- [ ] `PERP_VELOCITY` falls back to `ALONG` below the epsilon rather than stalling\n- [ ] `PERP_CORRIDOR` adds no speed along the gravity axis\n- [ ] `gravity.py` still imports nothing but `__future__`, `math`, `dataclasses`, `enum`\n\n**Verify:** `PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/pytest tests/test_gravity.py tests/test_purity.py -q` \u2192 all pass\n\n**Steps:** Write the seven failing tests; run them red; add `GravityMode(str, Enum)` with `.next()`, `PERP_VELOCITY_EPSILON`, and `apply_gravity`; widen the purity allow-list; run green; commit. Full code in the plan, section \"Task 1\".\n\n```json:metadata\n{\"files\": [\"src/gravi/gravity.py\", \"tests/test_gravity.py\", \"tests/test_purity.py\"], \"verifyCommand\": \"PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/pytest tests/test_gravity.py tests/test_purity.py -q\", \"acceptanceCriteria\": [\"GravityMode cycles with wraparound\", \"ALONG returns v + direction*magnitude*dt\", \"PERP_VELOCITY preserves |v| to 1e-9 over 10000 steps\", \"PERP_VELOCITY turns heading by magnitude/|v| rad/s\", \"PERP_VELOCITY falls back to ALONG below epsilon\", \"PERP_CORRIDOR adds no speed along the gravity axis\", \"gravity.py imports only __future__, math, dataclasses, enum\"]}\n```"
    },
    {
      "id": 11,
      "subject": "Task 11: sim.py \u2014 run the mode, and hold the rope",
      "status": "pending",
      "blockedBy": [
        10
      ],
      "description": "**Goal:** `World` applies gravity through the mode and holds a rigid rope at the radius recorded when the player grabbed.\n\n**Files:**\n- Modify: `src/gravi/sim.py`\n- Test: `tests/test_sim.py`\n\n**Acceptance Criteria:**\n- [ ] `World` takes `gravity_mode` and `rigid_rope`, both defaulting to today's behaviour\n- [ ] Every existing sim test still passes untouched\n- [ ] With gravity off, a rigid latch is uniform circular motion: radius AND speed both constant\n- [ ] The radial component of velocity is removed while rigid\n- [ ] Releasing a rigid rope restores free flight\n- [ ] Rigid mode does not apply to a repel latch\n- [ ] A gravity swap leaves the velocity vector unchanged\n\n**Verify:** `PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/pytest tests/test_sim.py -q` \u2192 all pass\n\n**Steps:** Write six failing tests; add the two settings and the rope state; call `apply_gravity` AFTER the node force; skip the spring while rigid; add `_update_rope` and `_constrain_to_rope`; clear the rope in `reset()`; full suite; commit. Full code in the plan, section \"Task 2\".\n\n```json:metadata\n{\"files\": [\"src/gravi/sim.py\", \"tests/test_sim.py\"], \"verifyCommand\": \"PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/pytest tests/ -q\", \"acceptanceCriteria\": [\"World takes gravity_mode and rigid_rope defaulting to shipped behaviour\", \"existing sim tests pass untouched\", \"rigid latch with gravity off is uniform circular motion\", \"radial velocity removed while rigid\", \"releasing a rigid rope restores free flight\", \"rigid does not apply to repel\", \"a gravity swap leaves the velocity vector unchanged\"]}\n```"
    },
    {
      "id": 12,
      "subject": "Task 12: main.py \u2014 G cycles the mode, T toggles the rope",
      "status": "pending",
      "blockedBy": [
        11
      ],
      "description": "**Goal:** The two experiments are reachable while playing, and the HUD says which state you are in.\n\n**Files:**\n- Modify: `main.py`\n- Modify: `src/gravi/render/hud.py:12`\n- Create: `/home/ddgg0/.claude/jobs/00519978/tmp/smoke_modes.py`\n\n**Acceptance Criteria:**\n- [ ] `G` cycles the gravity mode, `T` toggles the rope, both with a status flash\n- [ ] Both survive `R` (a new run), because they are loop state, not world state\n- [ ] The HUD run line names the mode, and says `rope rigid` when the rope is rigid\n- [ ] The help line mentions both keys\n\n**Verify:** `PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/python /home/ddgg0/.claude/jobs/00519978/tmp/smoke_modes.py` \u2192 prints a gravity_mode that is not `along`, `rigid_rope: True`, and no traceback\n\n**Steps:** Import `GravityMode`; add loop state; bind `K_g` and `K_t`; push both onto the world each frame after `apply_tunables`; extend the HUD run line and help line; write and run the headless smoke script; commit. Full code in the plan, section \"Task 3\".\n\n```json:metadata\n{\"files\": [\"main.py\", \"src/gravi/render/hud.py\"], \"verifyCommand\": \"PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/python /home/ddgg0/.claude/jobs/00519978/tmp/smoke_modes.py\", \"acceptanceCriteria\": [\"G cycles the gravity mode with a status flash\", \"T toggles the rigid rope with a status flash\", \"both survive R\", \"HUD run line names the mode and the rope\", \"help line mentions both keys\"]}\n```"
    },
    {
      "id": 13,
      "subject": "Task 13: rebuild both builds and hand them over",
      "status": "pending",
      "blockedBy": [
        12
      ],
      "description": "**Goal:** The author can play the experiments in both builds.\n\n**Files:** none (build and process only)\n\n**Acceptance Criteria:**\n- [ ] Full suite green\n- [ ] `build/web` rebuilt, and the packaged `src/gravi/gravity.py` contains `perp-velocity`\n- [ ] `tools/serve_web.py` serving on 8000, native window relaunched\n- [ ] Branch pushed\n\n**Verify:** `PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/pytest tests/ -q` \u2192 all pass, and the packaged-bundle zipfile check prints `True`\n\n**Steps:** Full suite; `pygbag --build main.py`; confirm the packaged bundle carries the new code (a timestamp is not proof); serve with `tools/serve_web.py 8000`; relaunch native; push. Full detail in the plan, section \"Task 4\".\n\n```json:metadata\n{\"files\": [], \"verifyCommand\": \"PYTHONPATH=src /home/ddgg0/projects/Gravi/.venv/bin/pytest tests/ -q\", \"acceptanceCriteria\": [\"full suite green\", \"packaged bundle contains perp-velocity\", \"server on 8000 and native relaunched\", \"branch pushed\"]}\n```"
    }
  ],
  "lastUpdated": "2026-08-20"
}
